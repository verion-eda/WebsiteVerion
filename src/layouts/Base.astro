---
export interface Props {
    title?: string;
    description?: string;
}

const {
    title = "Verion — GPU-Accelerated RTL Simulation",
    description = "Verion delivers 10x–50x faster RTL simulation by harnessing GPU acceleration. Drop-in replacement for CPU-based simulators with full debug visibility.",
} = Astro.props;
---

<!doctype html>
<html lang="en" class="scroll-smooth">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta name="description" content={description} />
        <title>{title}</title>

        <!-- Open Graph -->
        <meta property="og:type" content="website" />
        <meta property="og:title" content={title} />
        <meta property="og:description" content={description} />
        <meta property="og:url" content="https://verion-eda.com" />
        <meta property="og:image" content="https://verion-eda.com/hero-chip.jpg" />
        <meta property="og:site_name" content="Verion EDA" />

        <!-- Twitter Card -->
        <meta name="twitter:card" content="summary_large_image" />
        <meta name="twitter:title" content={title} />
        <meta name="twitter:description" content={description} />
        <meta name="twitter:image" content="https://verion-eda.com/hero-chip.jpg" />

        <!-- Fonts -->
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap"
            rel="stylesheet"
        />

        <!-- Favicon -->
        <link rel="icon" type="image/svg+xml" href="/chipicon.svg" />
        <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
        <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />

        <style>
            /* ── Tron Ares global styles ── */

            /* Perspective grid background */
            .tron-grid {
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                pointer-events: none;
                z-index: 0;
                overflow: hidden;
            }

            .tron-grid::before {
                content: "";
                position: absolute;
                top: -80px;
                left: 0;
                right: 0;
                bottom: 0;
                background-image:
                    linear-gradient(
                        rgba(180, 200, 220, 0.018) 1px,
                        transparent 1px
                    ),
                    linear-gradient(
                        90deg,
                        rgba(180, 200, 220, 0.018) 1px,
                        transparent 1px
                    );
                background-size: 80px 80px;
            }

            .tron-grid::after {
                content: "";
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: radial-gradient(
                    ellipse at 50% 0%,
                    transparent 0%,
                    #050507 70%
                );
            }

            /* Scroll-triggered reveal — initial state set here to prevent FOUC.
               GSAP ScrollTrigger animates these to their final state. */
            .reveal {
                opacity: 0;
                will-change: transform, opacity, filter;
            }
            html.no-js .reveal {
                opacity: 1;
            }

            /* Subtle card hover */
            .card-glow {
                position: relative;
                transition:
                    background-color 0.3s ease,
                    border-color 0.3s ease,
                    box-shadow 0.3s ease;
            }
            .card-glow:hover {
                background-color: #0a0a0e;
                border-color: rgba(0, 212, 255, 0.25);
            }

            /* Text glow — reserved for hero headline only */
            .text-glow {
                text-shadow: 0 0 24px rgba(0, 212, 255, 0.18);
            }

            .text-glow-subtle {
                text-shadow: none;
            }

            /* Subtle border */
            .border-glow {
                box-shadow: inset 0 0 0 1px rgba(0, 212, 255, 0.04);
            }

            /* Button — clean hover */
            .btn-neon {
                position: relative;
                transition: all 0.2s ease;
            }
            .btn-neon:hover {
                background-color: rgba(0, 212, 255, 0.08);
                border-color: rgba(0, 212, 255, 0.6);
            }

            /* Horizontal line divider */
            .neon-line {
                height: 1px;
                background: linear-gradient(
                    90deg,
                    transparent,
                    rgba(0, 212, 255, 0.18),
                    transparent
                );
            }

            /* Section connector lines */
            .section-connector {
                position: relative;
            }
            .section-connector::after {
                content: "";
                position: absolute;
                bottom: 0;
                left: 50%;
                transform: translateX(-50%);
                width: 60%;
                max-width: 600px;
                height: 1px;
                background: linear-gradient(
                    90deg,
                    transparent,
                    rgba(0, 212, 255, 0.15),
                    transparent
                );
            }

            /* Selection styling */
            ::selection {
                background: rgba(0, 212, 255, 0.2);
                color: #00e5ff;
            }
        </style>
    </head>
    <body class="bg-verion-bg text-white antialiased relative">
        <!-- Subtle grid background -->
        <div class="tron-grid"></div>

        <!-- Content -->
        <div class="relative z-10">
            <slot />
        </div>

        <script>
            import { gsap } from "gsap";
            import { ScrollTrigger } from "gsap/ScrollTrigger";

            gsap.registerPlugin(ScrollTrigger);

            const prefersReduced = window.matchMedia(
                "(prefers-reduced-motion: reduce)",
            ).matches;

            const reveals = gsap.utils.toArray<HTMLElement>(".reveal");

            reveals.forEach((el) => {
                // Honor legacy delay classes (reveal-delay-1 → 0.1s, etc.)
                const delayMatch = el.className.match(/reveal-delay-(\d+)/);
                const delay = delayMatch ? parseInt(delayMatch[1], 10) * 0.08 : 0;

                gsap.fromTo(
                    el,
                    {
                        opacity: 0,
                        y: prefersReduced ? 0 : 48,
                        filter: prefersReduced ? "none" : "blur(8px)",
                    },
                    {
                        opacity: 1,
                        y: 0,
                        filter: "blur(0px)",
                        duration: prefersReduced ? 0.3 : 1.1,
                        delay,
                        ease: "power3.out",
                        scrollTrigger: {
                            trigger: el,
                            start: "top 85%",
                            toggleActions: "play none none none",
                        },
                    },
                );
            });

            // Recalculate trigger positions once fonts have loaded
            if (document.fonts?.ready) {
                document.fonts.ready.then(() => ScrollTrigger.refresh());
            }
        </script>
    </body>
</html>
